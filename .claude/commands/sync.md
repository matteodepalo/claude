Sync the agent files in this repository with the actual files used by Claude Code.

The source of truth is `~/.claude/agents/`. Copy all agent files from there to this repository's `agents/` directory.

Steps:
1. Compare files in `~/.claude/agents/` with `agents/` in this repo
2. Copy any files from `~/.claude/agents/` that are newer or different to `agents/`
3. Report what was synced
4. If changes were made, commit and push to GitHub with a descriptive message
